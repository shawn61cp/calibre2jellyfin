* Move remainder of path operations from os.path calls to pathlib calls
* Use pathlib's neat / operator where appropriate


* I think it will be best to fork a separate project for the windows version.
